import math 


class proximitySensor: 

	objectDistance = 0


	def _init_(self, enable, disable, getData, getStatus):
		self.enable = enable
		self.disable = disable
		self.getData = getData
		self.getStatus = getStatus
		self.printData = printData


def enable(self):
	status = True

def disable(self):
	status = False

def getStatus(self):
	return status

def getData(self):
	for i in range (0.20, 7)
		for theta in range (boat.pos[] - position * math.sin(30), position + position * math.sin(30))
			if tiles(i,theta).elementType != water
					return True


	
